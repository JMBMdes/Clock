## IDF Component Manager Manifest File
dependencies:
  idf: ">=5.1"

  ## GC9A01 circular LCD driver (Espressif official)
  espressif/esp_lcd_gc9a01:
    version: "^1.0.0"

  ## LVGL port for esp_lcd panels (handles task, timer, flush callback)
  espressif/esp_lvgl_port:
    version: "^2.3.0"

  ## LVGL v8.3.x — pinned to match esp_lvgl_port v2 requirements
  lvgl/lvgl:
    version: "~8.3.0"
